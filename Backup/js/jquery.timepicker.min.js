! function(e) {
    "object" == typeof exports && exports && "object" == typeof module && module && module.exports === exports ? e(require("jquery")) : "function" == typeof define && define.amd ? define(["jquery"], e) : e(jQuery)
}(function(e) {
    function t(e) {
        var i = e[0];
        return i.offsetWidth > 0 && i.offsetHeight > 0
    }

    function n(i) {
        if (i.minTime && (i.minTime = T(i.minTime)), i.maxTime && (i.maxTime = T(i.maxTime)), i.durationTime && "function" != typeof i.durationTime && (i.durationTime = T(i.durationTime)), "now" == i.scrollDefault) i.scrollDefault = function() {
            return i.roundingFunction(T(new Date), i)
        };
        else if (i.scrollDefault && "function" != typeof i.scrollDefault) {
            var t = i.scrollDefault;
            i.scrollDefault = function() {
                return i.roundingFunction(T(t), i)
            }
        } else i.minTime && (i.scrollDefault = function() {
            return i.roundingFunction(i.minTime, i)
        });
        if ("string" === e.type(i.timeFormat) && i.timeFormat.match(/[gh]/) && (i._twelveHourTime = !0), !1 === i.showOnFocus && -1 != i.showOn.indexOf("focus") && i.showOn.splice(i.showOn.indexOf("focus"), 1), i.disableTimeRanges.length > 0) {
            for (var n in i.disableTimeRanges) i.disableTimeRanges[n] = [T(i.disableTimeRanges[n][0]), T(i.disableTimeRanges[n][1])];
            i.disableTimeRanges = i.disableTimeRanges.sort(function(e, i) {
                return e[0] - i[0]
            });
            for (n = i.disableTimeRanges.length - 1; n > 0; n--) i.disableTimeRanges[n][0] <= i.disableTimeRanges[n - 1][1] && (i.disableTimeRanges[n - 1] = [Math.min(i.disableTimeRanges[n][0], i.disableTimeRanges[n - 1][0]), Math.max(i.disableTimeRanges[n][1], i.disableTimeRanges[n - 1][1])], i.disableTimeRanges.splice(n, 1))
        }
        return i
    }

    function r(i) {
        var t = i.data("timepicker-settings"),
            n = i.data("timepicker-list");
        if (n && n.length && (n.remove(), i.data("timepicker-list", !1)), t.useSelect) var r = n = e("<select />", {
            class: "ui-timepicker-select"
        });
        else n = e("<ul />", {
            class: "ui-timepicker-list"
        }), (r = e("<div />", {
            class: "ui-timepicker-wrapper",
            tabindex: -1
        })).css({
            display: "none",
            position: "absolute"
        }).append(n);
        if (t.noneOption)
            if (!0 === t.noneOption && (t.noneOption = t.useSelect ? "Time..." : "None"), e.isArray(t.noneOption)) {
                for (var o in t.noneOption)
                    if (parseInt(o, 10) == o) {
                        var l = a(t.noneOption[o], t.useSelect);
                        n.append(l)
                    }
            } else {
                l = a(t.noneOption, t.useSelect);
                n.append(l)
            }
        t.className && r.addClass(t.className), null === t.minTime && null === t.durationTime || !t.showDuration || ("function" == typeof t.step || t.step, r.addClass("ui-timepicker-with-duration"), r.addClass("ui-timepicker-step-" + t.step));
        var p = t.minTime;
        "function" == typeof t.durationTime ? p = T(t.durationTime()) : null !== t.durationTime && (p = t.durationTime);
        var m = null !== t.minTime ? t.minTime : 0,
            f = null !== t.maxTime ? t.maxTime : m + w - 1;
        m > f && (f += w), f === w - 1 && "string" === e.type(t.timeFormat) && t.show2400 && (f = w);
        var h = t.disableTimeRanges,
            b = 0,
            y = h.length,
            H = t.step;
        "function" != typeof H && (H = function() {
            return t.step
        });
        o = m;
        for (var C = 0; f >= o; o += 60 * H(++C)) {
            var D, O = o,
                R = v(O, t);
            if (t.useSelect)(D = e("<option />", {
                value: R
            })).text(R);
            else(D = e("<li />")).data("time", 86400 >= O ? O : O % 86400), D.text(R);
            if ((null !== t.minTime || null !== t.durationTime) && t.showDuration) {
                var S = k(o - p, t.step);
                if (t.useSelect) D.text(D.text() + " (" + S + ")");
                else {
                    var M = e("<span />", {
                        class: "ui-timepicker-duration"
                    });
                    M.text(" (" + S + ")"), D.append(M)
                }
            }
            y > b && (O >= h[b][1] && (b += 1), h[b] && O >= h[b][0] && O < h[b][1] && (t.useSelect ? D.prop("disabled", !0) : D.addClass("ui-timepicker-disabled"))), n.append(D)
        }
        if (r.data("timepicker-input", i), i.data("timepicker-list", r), t.useSelect) i.val() && n.val(s(T(i.val()), t)), n.on("focus", function() {
            e(this).data("timepicker-input").trigger("showTimepicker")
        }), n.on("blur", function() {
            e(this).data("timepicker-input").trigger("hideTimepicker")
        }), n.on("change", function() {
            d(i, e(this).val(), "select")
        }), d(i, n.val(), "initial"), i.hide().after(n);
        else {
            var F = t.appendTo;
            "string" == typeof F ? F = e(F) : "function" == typeof F && (F = F(i)), F.append(r), u(i, n), n.on("mousedown", "li", function(t) {
                i.off("focus.timepicker"), i.on("focus.timepicker-ie-hack", function() {
                    i.off("focus.timepicker-ie-hack"), i.on("focus.timepicker", x.show)
                }), c(i) || i[0].focus(), n.find("li").removeClass("ui-timepicker-selected"), e(this).addClass("ui-timepicker-selected"), g(i) && (i.trigger("hideTimepicker"), n.on("mouseup.timepicker", "li", function(e) {
                    n.off("mouseup.timepicker"), r.hide()
                }))
            })
        }
    }

    function a(i, t) {
        var n, r, a;
        return "object" == typeof i ? (n = i.label, r = i.className, a = i.value) : "string" == typeof i ? n = i : e.error("Invalid noneOption value"), t ? e("<option />", {
            value: a,
            class: r,
            text: n
        }) : e("<li />", {
            class: r,
            text: n
        }).data("time", a)
    }

    function s(e, i) {
        return null !== (e = i.roundingFunction(e, i)) ? v(e, i) : void 0
    }

    function o(i) {
        var t = e(i.target);
        0 === t.closest(".ui-timepicker-input").length && 0 === t.closest(".ui-timepicker-wrapper").length && (x.hide(), e(document).unbind(".ui-timepicker"), e(window).unbind(".ui-timepicker"))
    }

    function c(e) {
        var i = e.data("timepicker-settings");
        return (window.navigator.msMaxTouchPoints || "ontouchstart" in document) && i.disableTouchKeyboard
    }

    function l(i, t, n) {
        if (!n && 0 !== n) return !1;
        var r = i.data("timepicker-settings"),
            a = !1;
        n = r.roundingFunction(n, r);
        return t.find("li").each(function(i, t) {
            var r = e(t);
            if ("number" == typeof r.data("time")) return r.data("time") == n ? (a = r, !1) : void 0
        }), a
    }

    function u(e, i) {
        i.find("li").removeClass("ui-timepicker-selected");
        var t = T(m(e), e.data("timepicker-settings"));
        if (null !== t) {
            var n = l(e, i, t);
            if (n) {
                var r = n.offset().top - i.offset().top;
                (r + n.outerHeight() > i.outerHeight() || 0 > r) && i.scrollTop(i.scrollTop() + n.position().top - n.outerHeight()), n.addClass("ui-timepicker-selected")
            }
        }
    }

    function p(i, t) {
        if ("" !== this.value && "timepicker" != t) {
            var n = e(this);
            if (!n.is(":focus") || i && "change" == i.type) {
                var r = n.data("timepicker-settings"),
                    a = T(this.value, r);
                if (null === a) return void n.trigger("timeFormatError");
                var s = !1;
                null !== r.minTime && a < r.minTime ? s = !0 : null !== r.maxTime && a > r.maxTime && (s = !0), e.each(r.disableTimeRanges, function() {
                    return a >= this[0] && a < this[1] ? (s = !0, !1) : void 0
                }), r.forceRoundTime && (a = r.roundingFunction(a, r));
                var o = v(a, r);
                s ? d(n, o, "error") && n.trigger("timeRangeError") : d(n, o)
            }
        }
    }

    function m(e) {
        return e.is("input") ? e.val() : e.data("ui-timepicker-value")
    }

    function d(e, i, t) {
        if (e.is("input")) {
            e.val(i);
            var n = e.data("timepicker-settings");
            n.useSelect && "select" != t && "initial" != t && e.data("timepicker-list").val(s(T(i), n))
        }
        return e.data("ui-timepicker-value") != i ? (e.data("ui-timepicker-value", i), "select" == t ? e.trigger("selectTime").trigger("changeTime").trigger("change", "timepicker") : "error" != t && e.trigger("changeTime"), !0) : (e.trigger("selectTime"), !1)
    }

    function f(i) {
        var n = e(this),
            r = n.data("timepicker-list");
        if (!r || !t(r)) {
            if (40 != i.keyCode) return !0;
            x.show.call(n.get(0)), r = n.data("timepicker-list"), c(n) || n.focus()
        }
        switch (i.keyCode) {
            case 13:
                return g(n) && x.hide.apply(this), i.preventDefault(), !1;
            case 38:
                var a = r.find(".ui-timepicker-selected");
                return a.length ? a.is(":first-child") || (a.removeClass("ui-timepicker-selected"), a.prev().addClass("ui-timepicker-selected"), a.prev().position().top < a.outerHeight() && r.scrollTop(r.scrollTop() - a.outerHeight())) : (r.find("li").each(function(i, t) {
                    return e(t).position().top > 0 ? (a = e(t), !1) : void 0
                }), a.addClass("ui-timepicker-selected")), !1;
            case 40:
                return 0 === (a = r.find(".ui-timepicker-selected")).length ? (r.find("li").each(function(i, t) {
                    return e(t).position().top > 0 ? (a = e(t), !1) : void 0
                }), a.addClass("ui-timepicker-selected")) : a.is(":last-child") || (a.removeClass("ui-timepicker-selected"), a.next().addClass("ui-timepicker-selected"), a.next().position().top + 2 * a.outerHeight() > r.outerHeight() && r.scrollTop(r.scrollTop() + a.outerHeight())), !1;
            case 27:
                r.find("li").removeClass("ui-timepicker-selected"), x.hide();
                break;
            case 9:
                x.hide();
                break;
            default:
                return !0
        }
    }

    function h(i) {
        var n = e(this),
            r = n.data("timepicker-list"),
            a = n.data("timepicker-settings");
        if (!r || !t(r) || a.disableTextInput) return !0;
        switch (i.keyCode) {
            case 96:
            case 97:
            case 98:
            case 99:
            case 100:
            case 101:
            case 102:
            case 103:
            case 104:
            case 105:
            case 48:
            case 49:
            case 50:
            case 51:
            case 52:
            case 53:
            case 54:
            case 55:
            case 56:
            case 57:
            case 65:
            case 77:
            case 80:
            case 186:
            case 8:
            case 46:
                a.typeaheadHighlight ? u(n, r) : r.hide()
        }
    }

    function g(e) {
        var i = e.data("timepicker-settings"),
            t = null,
            n = e.data("timepicker-list").find(".ui-timepicker-selected");
        return !n.hasClass("ui-timepicker-disabled") && (n.length && (t = n.data("time")), null !== t && ("string" != typeof t && (t = v(t, i)), d(e, t, "select")), !0)
    }

    function k(e, i) {
        e = Math.abs(e);
        var t, n, r = Math.round(e / 60),
            a = [];
        return 60 > r ? a = [r, y.mins] : (t = Math.floor(r / 60), n = r % 60, 30 == i && 30 == n && (t += y.decimal + 5), a.push(t), a.push(1 == t ? y.hr : y.hrs), 30 != i && n && (a.push(n), a.push(y.mins))), a.join(" ")
    }

    function v(i, t) {
        if (null === i) return null;
        var n = new Date(b.valueOf() + 1e3 * i);
        if (isNaN(n.getTime())) return null;
        if ("function" === e.type(t.timeFormat)) return t.timeFormat(n);
        for (var r, a, s = "", o = 0; o < t.timeFormat.length; o++) switch (a = t.timeFormat.charAt(o)) {
            case "a":
                s += n.getHours() > 11 ? y.pm : y.am;
                break;
            case "A":
                s += n.getHours() > 11 ? y.PM : y.AM;
                break;
            case "g":
                s += 0 === (r = n.getHours() % 12) ? "12" : r;
                break;
            case "G":
                r = n.getHours(), i === w && (r = 24), s += r;
                break;
            case "h":
                0 !== (r = n.getHours() % 12) && 10 > r && (r = "0" + r), s += 0 === r ? "12" : r;
                break;
            case "H":
                r = n.getHours(), i === w && (r = 24), s += r > 9 ? r : "0" + r;
                break;
            case "i":
                var c = n.getMinutes();
                s += c > 9 ? c : "0" + c;
                break;
            case "s":
                s += (i = n.getSeconds()) > 9 ? i : "0" + i;
                break;
            case "\\":
                o++, s += t.timeFormat.charAt(o);
                break;
            default:
                s += a
        }
        return s
    }

    function T(e, i) {
        if ("" === e) return null;
        if (!e || e + 0 == e) return e;
        if ("object" == typeof e) return 3600 * e.getHours() + 60 * e.getMinutes() + e.getSeconds();
        ("a" == (e = e.toLowerCase().replace(/[\s\.]/g, "")).slice(-1) || "p" == e.slice(-1)) && (e += "m");
        var t = "(" + y.am.replace(".", "") + "|" + y.pm.replace(".", "") + "|" + y.AM.replace(".", "") + "|" + y.PM.replace(".", "") + ")?",
            n = new RegExp("^" + t + "([0-2]?[0-9])\\W?([0-5][0-9])?\\W?([0-5][0-9])?" + t + "$"),
            r = e.match(n);
        if (!r) return null;
        var a = parseInt(1 * r[2], 10),
            s = r[1] || r[5],
            o = a;
        if (12 >= a && s) {
            var c = s == y.pm || s == y.PM;
            o = 12 == a ? c ? 12 : 0 : a + (c ? 12 : 0)
        }
        var l = 3600 * o + 60 * (1 * r[3] || 0) + (1 * r[4] || 0);
        if (12 > a && !s && i && i._twelveHourTime && i.scrollDefault) {
            var u = l - i.scrollDefault();
            0 > u && u >= w / -2 && (l = (l + w / 2) % w)
        }
        return l
    }
    var b = new Date(1970, 1, 1, 0, 0, 0),
        w = 86400,
        y = {
            am: "am",
            pm: "pm",
            AM: "AM",
            PM: "PM",
            decimal: ".",
            mins: "mins",
            hr: "hr",
            hrs: "hrs"
        },
        x = {
            init: function(t) {
                return this.each(function() {
                    var a = e(this),
                        s = [];
                    for (var o in e.fn.timepicker.defaults) a.data(o) && (s[o] = a.data(o));
                    var c = e.extend({}, e.fn.timepicker.defaults, s, t);
                    if (c.lang && (y = e.extend(y, c.lang)), c = n(c), a.data("timepicker-settings", c), a.addClass("ui-timepicker-input"), c.useSelect) r(a);
                    else {
                        if (a.prop("autocomplete", "off"), c.showOn)
                            for (i in c.showOn) a.on(c.showOn[i] + ".timepicker", x.show);
                        a.on("change.timepicker", p), a.on("keydown.timepicker", f), a.on("keyup.timepicker", h), c.disableTextInput && a.on("keypress.timepicker", function(e) {
                            e.preventDefault()
                        }), p.call(a.get(0))
                    }
                })
            },
            show: function(i) {
                var n = e(this),
                    a = n.data("timepicker-settings");
                if (i && i.preventDefault(), a.useSelect) n.data("timepicker-list").focus();
                else {
                    c(n) && n.blur();
                    var s = n.data("timepicker-list");
                    if (!n.prop("readonly") && (s && 0 !== s.length && "function" != typeof a.durationTime || (r(n), s = n.data("timepicker-list")), !t(s))) {
                        n.data("ui-timepicker-value", n.val()), u(n, s), x.hide(), s.show();
                        var p = {};
                        a.orientation.match(/r/) ? p.left = n.offset().left + n.outerWidth() - s.outerWidth() + parseInt(s.css("marginLeft").replace("px", ""), 10) : p.left = n.offset().left + parseInt(s.css("marginLeft").replace("px", ""), 10), "t" == (a.orientation.match(/t/) ? "t" : a.orientation.match(/b/) ? "b" : n.offset().top + n.outerHeight(!0) + s.outerHeight() > e(window).height() + e(window).scrollTop() ? "t" : "b") ? (s.addClass("ui-timepicker-positioned-top"), p.top = n.offset().top - s.outerHeight() + parseInt(s.css("marginTop").replace("px", ""), 10)) : (s.removeClass("ui-timepicker-positioned-top"), p.top = n.offset().top + n.outerHeight() + parseInt(s.css("marginTop").replace("px", ""), 10)), s.offset(p);
                        var d = s.find(".ui-timepicker-selected");
                        if (d.length || (m(n) ? d = l(n, s, T(m(n))) : a.scrollDefault && (d = l(n, s, a.scrollDefault()))), d && d.length) {
                            var f = s.scrollTop() + d.position().top - d.outerHeight();
                            s.scrollTop(f)
                        } else s.scrollTop(0);
                        return a.stopScrollPropagation && e(document).on("wheel.ui-timepicker", ".ui-timepicker-wrapper", function(i) {
                            i.preventDefault();
                            var t = e(this).scrollTop();
                            e(this).scrollTop(t + i.originalEvent.deltaY)
                        }), e(document).on("touchstart.ui-timepicker mousedown.ui-timepicker", o), e(window).on("resize.ui-timepicker", o), a.closeOnWindowScroll && e(document).on("scroll.ui-timepicker", o), n.trigger("showTimepicker"), this
                    }
                }
            },
            hide: function(i) {
                var n = e(this),
                    r = n.data("timepicker-settings");
                return r && r.useSelect && n.blur(), e(".ui-timepicker-wrapper").each(function() {
                    var i = e(this);
                    if (t(i)) {
                        var n = i.data("timepicker-input"),
                            r = n.data("timepicker-settings");
                        r && r.selectOnBlur && g(n), i.hide(), n.trigger("hideTimepicker")
                    }
                }), this
            },
            option: function(i, t) {
                return this.each(function() {
                    var a = e(this),
                        s = a.data("timepicker-settings"),
                        o = a.data("timepicker-list");
                    if ("object" == typeof i) s = e.extend(s, i);
                    else if ("string" == typeof i && void 0 !== t) s[i] = t;
                    else if ("string" == typeof i) return s[i];
                    s = n(s), a.data("timepicker-settings", s), o && (o.remove(), a.data("timepicker-list", !1)), s.useSelect && r(a)
                })
            },
            getSecondsFromMidnight: function() {
                return T(m(this))
            },
            getTime: function(e) {
                var i = m(this);
                if (!i) return null;
                var t = T(i);
                if (null === t) return null;
                e || (e = new Date);
                var n = new Date(e);
                return n.setHours(t / 3600), n.setMinutes(t % 3600 / 60), n.setSeconds(t % 60), n.setMilliseconds(0), n
            },
            setTime: function(e) {
                var i = this,
                    t = i.data("timepicker-settings");
                if (t.forceRoundTime) var n = s(T(e), t);
                else n = v(T(e), t);
                return e && null === n && t.noneOption && (n = e), d(i, n), i.data("timepicker-list") && u(i, i.data("timepicker-list")), this
            },
            remove: function() {
                var e = this;
                if (e.hasClass("ui-timepicker-input")) {
                    var i = e.data("timepicker-settings");
                    return e.removeAttr("autocomplete", "off"), e.removeClass("ui-timepicker-input"), e.removeData("timepicker-settings"), e.off(".timepicker"), e.data("timepicker-list") && e.data("timepicker-list").remove(), i.useSelect && e.show(), e.removeData("timepicker-list"), this
                }
            }
        };
    e.fn.timepicker = function(i) {
        return this.length ? x[i] ? this.hasClass("ui-timepicker-input") ? x[i].apply(this, Array.prototype.slice.call(arguments, 1)) : this : "object" != typeof i && i ? void e.error("Method " + i + " does not exist on jQuery.timepicker") : x.init.apply(this, arguments) : this
    }, e.fn.timepicker.defaults = {
        className: null,
        minTime: null,
        maxTime: null,
        durationTime: null,
        step: 30,
        showDuration: !1,
        showOnFocus: !0,
        showOn: ["click", "focus"],
        timeFormat: "g:i A",
        scrollDefault: null,
        selectOnBlur: !1,
        disableTextInput: !1,
        disableTouchKeyboard: !1,
        forceRoundTime: !1,
        roundingFunction: function(e, i) {
            if (null === e) return null;
            var t = e % (60 * i.step);
            return t >= 30 * i.step ? e += 60 * i.step - t : e -= t, e
        },
        appendTo: "body",
        orientation: "l",
        disableTimeRanges: [],
        closeOnWindowScroll: !1,
        typeaheadHighlight: !0,
        noneOption: !1,
        show2400: !1,
        stopScrollPropagation: !1
    }
});